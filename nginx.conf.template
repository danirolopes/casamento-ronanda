server {
    listen ${PORT};
    server_name _;

    location = /custom.css {
        alias /usr/share/nginx/html/custom.css;
        add_header Cache-Control "public, max-age=300";
    }

    location = /inject.js {
        alias /usr/share/nginx/html/inject.js;
        add_header Cache-Control "public, max-age=300";
    }

    location / {
        proxy_pass https://${CASAR_HOST}${CASAR_PATH}/;

        proxy_ssl_server_name on;

        proxy_set_header Host ${CASAR_HOST};
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        proxy_set_header Accept-Encoding "";

        proxy_cookie_domain .casar.com $host;

        proxy_redirect off;

        sub_filter_once off;
        sub_filter_types text/html;

        sub_filter '</head>' '<link rel="stylesheet" href="/custom.css"><script defer src="/inject.js"></script></head>';
    }
}
